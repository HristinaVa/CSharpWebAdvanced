@model IEnumerable<ImageViewModel>
@{
    ViewData["Title"] = "Home Page";
    var images = Model.ToList();
}
@if (!images.Any())
{
    <div class="mt-4 p-5 bg-light">
        <h1 class="display-4">Детска градина "Зайо Байо" Община Варна</h1>
        <p class="lead">
            Добре дошли в нашият чуден свят!
        </p>
        <hr class="my-4">
        <p class="lead">
           @*  @if (User.Identity?.IsAuthenticated ?? false)
            {
                <a asp-controller="House" asp-action="Add" class="btn btn-primary btn-lg"
                   role="button">Add House</a>
                   }
             *@
        </p>
    </div>
}
<div id="carouselExampleCaptions" class="carousel slide">
    <div class="carousel-indicators">
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2"></button>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3"></button>
    </div>
    <div class="carousel-inner">
        @for (int i = 0; i < images.Count(); i++)
 {
            var image = images[i];

            <div class="carousel-item @(i == 0 ? "active" : string.Empty)">
                <img src="@image.Url" class="d-block w-100" alt="@image.KindergartenName">
            <div class="carousel-caption d-none d-md-block">
                    <h5>Детска градина "@image.KindergartenName" Община Варна</h5>
                    <p>Добре дошли в нашият чуден свят!</p>
            </div>
        </div>
 }
        
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</div>
<div class="mb-5"></div>

<div class="row">
    <div class="col-12 text-center">
        <button class="btn btn-primary" id="statistics_btn">Show Statistics</button>
    </div>
</div>

<div class="mt-4 p-5 bg-light d-none" id="statistics_box">
    <div class="row">
        <h2 class="col-md-4 text-center" id="total_ageGroups"></h2>
        <h2 class="col-md-4 text-center" id="total_classGroups"></h2>
        <h2 class="col-md-4 text-center" id="total_children"></h2>
    </div>
</div>

@section Scripts
{
    <script>statistics();</script>
}